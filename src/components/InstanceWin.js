import React from 'react';

function InstanceWin(props) {
	return (
		<button className="game-win" onClick={props.onClick}>
			Test: Cheat Win Button
		</button>		
	)
}

export default InstanceWin;